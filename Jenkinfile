pipeline { 
  agent any
  stages {
    stage ('Build') {
      steps {
        sh 'printenv'
        sh 'docker build -t nilotpal123/jenkins:""$BUILD_ID"" .'
      }
    }
     stage ('Publish') {
      steps {
        sh 'docker push nilotpal123/jenkins:""$BUILD_ID""'
      }
    } 
}
}
